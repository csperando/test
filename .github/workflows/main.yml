on: [push]

jobs:
  hello:
    runs-on: ubuntu-latest
    name: hello
    steps:
      - name: hello
        id: hello
        uses: csperando/test@v1
        with:
          who-to-greet: "coleman"
      - name: output
        run: echo "The time was ${{ steps.hello.outputs.time }}"
